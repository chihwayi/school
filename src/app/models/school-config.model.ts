export interface SchoolConfig {
  id?: number;
  name: string;
  description: string;
  primaryColor: string;
  secondaryColor: string;
  contactEmail: string;
  contactPhone: string;
  address: string;
  website: string;
  logoPath?: string;
  backgroundPath?: string;
}
